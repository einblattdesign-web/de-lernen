DATABASE_URL="postgresql://user:password@localhost:5432/de_lernen"

# Optional: set both to require HTTP Basic Auth (recommended once this app
# is reachable from the public internet, e.g. deployed on Vercel). Leave
# unset for local development to skip the login prompt.
# BASIC_AUTH_USER="your-username"
# BASIC_AUTH_PASSWORD="your-password"
